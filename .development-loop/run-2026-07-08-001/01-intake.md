# 01 — Intake

Answered inline from this session's conversation (Arijit already scoped + approved via "yes do that now") — not re-interviewed, to avoid re-litigating a decision already made.

1. **Business problem:** the current Generic→Technical handoff is a regex-scanned prose sentinel (`[[HANDOFF:technical]]`) and the follow-up-question mechanism is another regex-scanned sentinel (`[[FOLLOWUP:...]]`). Both are model-compliance-dependent (LLM might not emit the exact string) and neither is a real platform primitive — fragile, and Technical never had a working follow-up at all. Cost: contributes to "Generic's minor prose quirk" bug still open, and blocks the case for adopting Algolia's own frontend stack (Arijit's standing order: use native Algolia tech, not custom equivalents).
2. **Users:** internal (Arijit, prospect demo audience for Adobe/Spectrum sales motion). Today they get a UI that occasionally fails to hand off or fails to suggest a follow-up because the model didn't emit the magic string.
3. **Success in numbers:** tool-call fires deterministically on live queries that warrant a specialist (spike already proved 5/5 + 2 independent live round trips); suggestion shown after every answer names a real prop/component from that turn's actual retrieved hits (not a generic template) — verified across a small live sample (>=5 turns), not just unit-tested.
4. **Explicitly NOT in scope:** the `react-instantsearch <Chat>` widget UI migration (separate, larger, later decision — needs its own `frontend-builder` design pass); the full 100-question eval run (tracked separately in SESSION.md); any change to `main` or the live-linked production agent IDs — this stays on `spike/agent-to-agent-tool` (or a branch off it) against dev/spike agent copies only.
5. **Compliance:** none apply — internal sales-demo tool, no PII/PCI/HIPAA/GDPR-regulated data in the corpus (Adobe Spectrum public docs).
6. **Systems touched:** Algolia Agent Studio's live REST API (agent create/update/publish, tool registration) — an external, third-party API surface. This is the fact that drives the scope classification below. Also touches: `web/src/lib/agentStudio.ts`, `web/src/hooks/useChat.ts`, `web/src/components/DiscoveryCard.tsx` (frontend), `scripts/agents/build_acs_agents.mjs`, `scripts/agents/instructions_generic.md`/`instructions_technical.md` (agent config scripts).
7. **Architectural constraints:** must preserve the human-gated deep-dive UX pattern (Arijit's 2026-07-03 RC2-alignment decision — no auto-fired second answer, user must opt in). Must not touch the live-linked agent IDs in `web/src/config/instances/spectrum.ts` without explicit redeploy sign-off. Must keep zero production risk per the spike's own precedent (branch-isolated, disposable probe agents only).
8. **Unknowns before spec:** none blocking — see `00-discovery.md`. One item (`include_tool_outputs` grounding behavior) is carried forward as a Build/Validate verification item, not a spec blocker, since the config field exists and is documented; only its *effect* is unverified.

## Scope classification: **FULL**

**Reasoning:** the gate is technical-surface, not effort or blast-radius. This work registers a new tool schema and changes agent `config` on Algolia's live external Agent Studio API (dimension 6) — that alone satisfies "touches external API surface," which triggers FULL regardless of how well-scoped or low-risk the change is otherwise.

**Compression applied (documented, not silent):** Discovery (Stage 0) is satisfied by the live spike already run this session and the prior one — cited above, not re-derived. Risk (Stage 2), Spec (Stage 3), and Architecture Review (Stage 4) still get their own artifacts next, but draw directly on the spike's empirical findings rather than re-researching from docs. This keeps the FULL-path paper trail intact (nothing silently skipped) while not duplicating work already done and verified live.
